if (-1 || 0) alert( 'first' ); // выполнится, тк -1 это true
if (-1 && 0) alert( 'second' ); // не выполнится, тк 0 это false
if (null || -1 && 1) alert( 'third' ); // выполнится, тк у && больший приоритет то в условии будет null || 1