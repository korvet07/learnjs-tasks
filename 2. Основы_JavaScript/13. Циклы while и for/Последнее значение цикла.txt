let i = 3;

while (i) {
  alert( i-- );
}                // Выведет 3, 2, 1 тк постфиксный декремент в теле цикла возвращает "старое" значение, а потом             				уменьшает его на 1, при i=0 цикл прервётся.