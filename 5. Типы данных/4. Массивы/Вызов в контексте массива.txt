let arr = ["a", "b"];

arr.push(function() {
  alert( this );
});

arr[2](); // выведет массив

Тк массивы разновидность объектов, по сути был добавлен метод, который при вызове как метод определит this как сам массив.